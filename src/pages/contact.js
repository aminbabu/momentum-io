import React from "react";
import Layout from "../layouts/Layout";

const ContactPage = () => {
  return <Layout isDark="true">Contact Page</Layout>;
};

export default ContactPage;

export const Head = () => <title>Contact Page</title>;
